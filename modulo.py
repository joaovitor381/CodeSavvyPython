# Import random
from random import random, choice  # choice retorna um elemento aleatório

print(random())

lista = ["carbono", "nitrogenio", "oxigenio"]
print(choice(lista))  # Usando "random" faz gerar valores aleatórios
